namespace Grooming.Core.Constants;

public static class DevConstants
{
    // A unique ID for local testing that won't collide with real tenants
    public const string LocalDevTenantId = "dev-test-tenant-2026";
}